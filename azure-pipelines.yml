# Starter pipeline for self-hosted Mac agent

trigger:
- main  # or your branch name

# Use self-hosted agent pool
pool:
  name: Default

steps:
# Simple one-line script to test the agent
- script: echo "Hello World from Mac Agent!"
  displayName: 'Run a one-line script'

# Multi-line script for future tasks (build/test/deploy)
- script: |
    echo "Add other tasks to build, test, and deploy your project."
    echo "See https://aka.ms/yaml for guidance."
  displayName: 'Run a multi-line script'